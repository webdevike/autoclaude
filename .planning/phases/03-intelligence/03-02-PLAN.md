---
phase: 03-intelligence
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/core/src/agent.ts
  - packages/gateway/src/index.ts
  - config/work.json
autonomous: true

must_haves:
  truths:
    - "Conversation history persists across restarts with retention policy"
    - "Work and personal modes have separate credentials and system prompts"
    - "User can switch modes via Telegram command without restarting"
  artifacts:
    - path: "packages/core/src/agent.ts"
      provides: "SessionManager with retention policy, mode switching without restart"
      contains: "enforceRetentionPolicy"
    - path: "config/work.json"
      provides: "Work mode configuration with separate settings"
      contains: "work"
  key_links:
    - from: "packages/gateway/src/index.ts"
      to: "packages/core/src/agent.ts"
      via: "/mode command handling"
      pattern: "/mode"
---

<objective>
Enable dynamic mode switching and conversation history retention, completing the Intelligence phase.

Purpose: Allow on-the-fly mode switching without gateway restart, and prevent unbounded conversation history growth. This completes the memory and mode requirements.

Output: Mode switching via /mode command reloads configs, retention policy cleans old messages automatically, work mode config with separate settings.
</objective>

<execution_context>
@/Users/ike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligence/03-RESEARCH.md
# Note: 03-01-SUMMARY.md will exist after 03-01 execution completes

# Existing infrastructure:
# - SessionManager in agent.ts handles JSONL persistence (Phase 1)
# - Mode configs loaded from config/*.json at startup
# - /mode command exists but only switches activeMode variable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retention policy to SessionManager</name>
  <files>packages/core/src/agent.ts</files>
  <action>
Enhance SessionManager with configurable retention policy:

**Add RetentionPolicy interface:**
```typescript
interface RetentionPolicy {
  maxMessages: number;     // Default: 50
  maxAgeDays: number;      // Default: 30
  minMessages: number;     // Default: 10 (always keep at least this many)
}
```

**Modify SessionManager:**
1. Accept RetentionPolicy in constructor (use defaults if not provided)
2. Add `enforceRetentionPolicy()` method:
   - Load all messages
   - If count <= minMessages, skip cleanup
   - Filter by maxAgeDays and take last maxMessages
   - Ensure at least minMessages kept
   - If filtered count < original, rewrite file with kept messages
   - Use atomic write (temp file + rename)
3. Call `enforceRetentionPolicy()` after each `appendSession()` call
4. Add `getStats()` method returning { total, oldest, newest } timestamps

**Safe JSONL read:**
- Parse line-by-line, skip invalid entries with warning
- If >10% invalid lines, backup corrupt file and rewrite with valid messages

Log retention actions: "[history] Retention: {before} → {after} messages"
  </action>
  <verify>
Build succeeds: `cd /Users/ike/Code/autoclaude && pnpm build`
  </verify>
  <done>SessionManager enforces retention policy, prevents unbounded history growth</done>
</task>

<task type="auto">
  <name>Task 2: Enable dynamic mode switching without restart</name>
  <files>packages/core/src/agent.ts, packages/gateway/src/index.ts</files>
  <action>
Make mode switching reload configs from disk without process restart:

**In AgentOrchestrator (agent.ts):**
1. Add `configDir: string` property set via constructor or default to `config/`
2. Add `loadAllModes()` method that reads all JSON files from configDir
3. Add `reloadModes()` public method that calls loadAllModes()
4. Modify `switchMode(mode: string)`:
   - First call reloadModes() to pick up any config changes
   - Then set activeMode if mode exists
   - Return success/failure message with available modes
5. Add `getAvailableModes()` returning array of mode names
6. Add `getCurrentModeConfig()` returning current mode's config

**In Gateway (gateway.ts):**
1. Modify /mode command handling in handleIncoming or add to orchestrator.handleCommand:
   - `/mode` or `/modes`: List current mode and available modes
   - `/mode <name>`: Switch to specified mode, show confirmation
   - Show available integrations and tone for new mode

**Session-locked config:**
- Capture mode config at start of handleMessage()
- Use captured config throughout the message processing
- Don't allow mode to change mid-session
  </action>
  <verify>
1. Build succeeds: `pnpm build`
2. Test mode switching:
   - Send `/mode` to see current mode
   - Send `/mode work` to switch (if work.json exists)
   - Verify new mode takes effect for next message
3. Test config reload from disk:
   - While gateway is running, edit config/work.json (e.g., change tone to "formal")
   - Send `/mode personal` then `/mode work`
   - Send "What is your tone?" or check logs
   - Verify the edited config value is now active (proves reload worked)
  </verify>
  <done>Mode switching reloads configs from disk without restart</done>
</task>

<task type="auto">
  <name>Task 3: Create work mode config and apply cwd in AgentOrchestrator</name>
  <files>config/work.json, packages/core/src/agent.ts</files>
  <action>
**Create work mode configuration (config/work.json):**

```json
{
  "mode": "work",
  "systemPrompt": "You are Jarvis in work mode. Be professional, concise, and focused on productivity. Prioritize efficiency and clarity in all responses.",
  "tone": "professional",
  "triage": {
    "model": "openrouter/google/gemini-2.5-flash-lite",
    "maxTokens": 1024
  },
  "smart": {
    "model": "openrouter/anthropic/claude-sonnet-4",
    "maxTokens": 8192
  },
  "channels": ["telegram"],
  "integrations": ["linear", "gmail"],
  "cwd": "/home/ike/workspace",
  "statusInterval": 300,
  "crons": []
}
```

Key differences from personal.json:
- systemPrompt emphasizes professional, concise responses
- tone: "professional" (personal is "casual")
- integrations: ["linear", "gmail"] (work tools vs personal's ["notion", "gmail"])
- cwd: "/home/ike/workspace" (work directory)

**Apply cwd in AgentOrchestrator (agent.ts):**

In `handleMessage()` or `runSmartAgent()`, apply the mode's cwd:
1. Read `modeConfig.cwd` from the current mode config
2. If cwd is defined and exists (fs.existsSync):
   - Set `process.env.AGENT_CWD = modeConfig.cwd`
   - Pass cwd to any tools that spawn processes (e.g., shell tools)
   - Log: `[mode] Working directory: ${modeConfig.cwd}`
3. If cwd is defined but doesn't exist:
   - Log warning: `[mode] Configured cwd does not exist: ${modeConfig.cwd}`
   - Continue with default cwd

Note: Since JSON doesn't support env vars, handle cwd fallback in loadAllModes():
```typescript
if (config.cwd) {
  config.cwd = config.cwd.replace('${WORK_CWD}', process.env.WORK_CWD || config.cwd);
}
```
  </action>
  <verify>
1. File exists and is valid JSON: `cat config/work.json | jq .`
2. Mode can be loaded: Start gateway and send `/mode work`
3. cwd is applied:
   - With LOG_LEVEL=debug, after `/mode work`, check logs for "[mode] Working directory:"
   - Or set AGENT_CWD env var inspection in handleMessage
  </verify>
  <done>
- Work mode config with separate credentials, system prompt, and integrations
- cwd from mode config is applied when mode is active
  </done>
</task>

</tasks>

<verification>
1. Build passes: `cd /Users/ike/Code/autoclaude && pnpm build`
2. Retention policy works:
   - Create test session with >50 messages
   - Verify cleanup reduces to maxMessages
   - Check minMessages preserved
3. Mode switching works:
   - `/mode` shows current mode and available modes
   - `/mode work` switches and confirms
   - Next message uses work mode config
4. Work config distinct:
   - Different system prompt
   - Different integrations
   - Professional tone
</verification>

<success_criteria>
- Conversation history enforces retention (max 50 messages, 30 days, min 10)
- Mode switch via `/mode <name>` reloads config from disk
- Work and personal modes have distinct system prompts, integrations, and tone
- No gateway restart required for mode switch
- Session captures mode config at start (no mid-session switches)
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence/03-02-SUMMARY.md`
</output>
