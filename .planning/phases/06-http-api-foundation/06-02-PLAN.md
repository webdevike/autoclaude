---
phase: 06-http-api-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/cli/src/index.ts
  - packages/cli/package.json
autonomous: true

must_haves:
  truths:
    - "HTTP API starts automatically when CLI starts the gateway process"
    - "HTTP API starts after gateway.start() completes (channels ready first)"
    - "No manual intervention required — startHttpApi called in main() function"
  artifacts:
    - path: "packages/cli/src/index.ts"
      provides: "startHttpApi call wired into startup sequence after gateway.start()"
      contains: "startHttpApi"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/gateway/src/http-api.ts"
      via: "import { startHttpApi } from '@jarvis/gateway'"
      pattern: "startHttpApi"
    - from: "packages/cli/src/index.ts"
      to: "packages/core/src/agent.ts"
      via: "passes orchestrator instance to startHttpApi"
      pattern: "startHttpApi.*orchestrator"
---

<objective>
Wire the HTTP API into the CLI startup sequence so it starts automatically when the gateway process launches.

Purpose: The HTTP API must start without manual intervention. The CLI already creates the orchestrator and starts the gateway — this plan adds one call to `startHttpApi()` after `gateway.start()` so the LiveKit agent can immediately communicate with the orchestrator.

Output: Updated `packages/cli/src/index.ts` with HTTP API startup integrated into the main() function.
</objective>

<execution_context>
@/Users/ike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-http-api-foundation/06-01-SUMMARY.md

@packages/cli/src/index.ts
@packages/cli/package.json
@packages/gateway/src/http-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire startHttpApi into CLI startup and verify end-to-end</name>
  <files>
    packages/cli/src/index.ts
    packages/cli/package.json
  </files>
  <action>
1. In `packages/cli/src/index.ts`, update the import from `@jarvis/gateway` to include `startHttpApi`:

Change:
```typescript
import { Gateway } from "@jarvis/gateway";
```
To:
```typescript
import { Gateway, startHttpApi } from "@jarvis/gateway";
```

2. In the `main()` function, add the `startHttpApi` call AFTER the `await gateway.start()` line (line ~145 in current code) and BEFORE the autonomous runner setup. Insert:

```typescript
  // --- Start HTTP API for LiveKit agent communication ---
  await startHttpApi({ orchestrator });
```

This passes the orchestrator instance (which already has all integrations, tools, modes, and workspace configured) to the HTTP API. The default port (3457) and host (127.0.0.1) from the http-api module will be used.

3. Verify `packages/cli/package.json` already has `@jarvis/gateway` in dependencies (it does — `"@jarvis/gateway": "workspace:*"`). No changes needed to package.json.

Important notes:
- Place the startHttpApi call AFTER `gateway.start()` because the gateway needs its channels initialized first.
- Place it BEFORE the autonomous runner setup since the runner doesn't depend on the HTTP API.
- The orchestrator passed to startHttpApi is the same instance that handles Telegram messages, ensuring identical code path (same tools, same SOUL.md, same session continuity, same modes).
- Do NOT pass port or host — let the defaults (3457, 127.0.0.1) be used. These can be made configurable via env vars in a future iteration if needed.
  </action>
  <verify>
1. Run `cd /Users/ike/Code/ironman/jarvis && pnpm type-check` to verify the full monorepo type-checks successfully.
2. Visually confirm the startup order in main(): config → workspace → orchestrator → integrations → gateway → channels → scheduler → **HTTP API** → autonomous runner.
3. Grep for `startHttpApi` in cli/src/index.ts to confirm it's called with `{ orchestrator }`.
  </verify>
  <done>
CLI's main() calls startHttpApi({ orchestrator }) after gateway.start(). Full monorepo type-check passes. HTTP API will start automatically on localhost:3457 when the CLI process launches.
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check` passes for the entire monorepo (all packages)
2. `packages/cli/src/index.ts` imports `startHttpApi` from `@jarvis/gateway`
3. `startHttpApi({ orchestrator })` is called after `gateway.start()` in main()
4. The orchestrator instance passed is the fully-configured one with integrations, tools, and workspace
5. No other files modified — clean single-point integration
</verification>

<success_criteria>
- HTTP API starts automatically when CLI starts the gateway process
- No manual steps required — fully automated startup
- Same orchestrator instance used for both Telegram and HTTP API paths
- Full monorepo type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-http-api-foundation/06-02-SUMMARY.md`
</output>
