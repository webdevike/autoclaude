{
  "name": "jarvis",
  "version": "0.1.0",
  "private": true,
  "description": "Personal AI agent with multi-channel messaging, smart delegation, and integrations",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @jarvis/cli dev",
    "start": "pnpm --filter @jarvis/cli start",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
